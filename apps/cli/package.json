{
  "name": "@ui-buddy/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "ui-buddy": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests",
    "format": "prettier --write . --ignore-path ../../.prettierignore"
  },
  "dependencies": {
    "@ui-buddy/adapters": "workspace:*",
    "@ui-buddy/core": "workspace:*",
    "@ui-buddy/shared": "workspace:*",
    "chalk": "^5.0.0",
    "commander": "^14.0.0"
  },
  "devDependencies": {
    "@ui-buddy/config": "workspace:*"
  }
}
