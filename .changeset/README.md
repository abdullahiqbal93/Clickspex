# Changesets

Use `pnpm changeset` for every release-affecting change. The packages are fixed together so the CLI, extension, MCP server, shared protocol, core utilities, and adapters keep one Clickspex product version.
